defmodule PhxMolbindWeb.ComponentsLive.GeneratedMoleculeList do
  use PhxMolbindWeb, :live_component

  def render(assigns) do
    ~H"""
    <div >
    </div>

    """
  end

  def update(assigns, socket) do
    {:ok, socket |> assign(assigns)}
  end
end
